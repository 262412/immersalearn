"use client";

// PlayerController is now replaced by OrbitControls in SceneRenderer.
// This file is kept for backward compatibility but exports nothing.
// The orbit camera + direct onClick on meshes replaces the old
// WASD + pointer lock + raycast system.

export function PlayerController() {
  return null;
}
